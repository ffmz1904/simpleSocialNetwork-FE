import React from 'react';
import {Container} from "semantic-ui-react";

const Settings = () => {
    return (
        <div className="page">
            <Container>
                settings
            </Container>
        </div>
    );
};

export default Settings;
